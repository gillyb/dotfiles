neovim
------
This will install neovim.
It will run 'brew install neovim'

If you would like to use the latest (and possibly unstable)
version of neovim, then run 'brew install --HEAD neovim'
(This might be needed for newer features, like 'floating windows')

This will copy vim settings files, and install all
plugins needed for neovim.
